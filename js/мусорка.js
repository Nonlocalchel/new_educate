	/* более понятный обработик действий мышки
		key.addEventListener('mouseover',function(event){
			let targetForMouse=event.target.closest('.results__card');
			let targetForMouseId=targetForMouse.getAttribute('id');
			//console.log(targetForMouseId);
			change_shadow(targetForMouseId);
		});
		key.addEventListener('mouseout',function(event){
			let targetForMouse=event.target.closest('.results__card');
			let targetForMouseId=targetForMouse.getAttribute('id');
			//console.log(targetForMouseId);
			change_shadow(targetForMouseId);
		});
		//key.style.boxShadow=`0px 6px 20px 0px ${color}, 0.20)`;
		*/
